<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    android:background="@color/blue"
    android:orientation="vertical">

    <!--
        <TextView
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="12dp"
            android:textSize="15sp"
            android:textColor="@color/grey"
            android:gravity="center_horizontal"
            android:layout_alignParentBottom="true"
            android:text="Отсутствует соединение с интернетом" />
     -->
    <!--
        <EditText
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Элез"
            android:textColorHint="@color/greylite"
            android:layout_marginLeft="10dp"
            android:layout_marginRight="10dp"
            android:id="@+id/editText"
            android:textColor="#ffffff" />
     -->
    <SearchView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginLeft="10dp"
        android:layout_marginRight="10dp"
        android:id="@+id/searchView"
        android:queryHint="Введите игровое имя персонажа" />

    <!-- android:focusable="true" -->
    <!-- android:iconifiedByDefault="false" -->

    <TextView
        android:id="@+id/start_textView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginLeft="10dp"
        android:layout_marginRight="10dp"
        android:textSize="15sp"
        android:layout_marginTop="20dp"
        android:text="Для просмотра статистики введите имя персонажа. К примеру введите в поле Элез"
        android:textColor="@color/grey"
        android:layout_below="@+id/start_imageView"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:focusable="false" />

    </LinearLayout>